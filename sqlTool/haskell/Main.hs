module Main where

import Data.Map (Map)
import Data.Set (Set)
import Data.List (nub)
import Types
--
--instance MyBuilderClass QueryBuilder where
--  appendString str (QueryBuilder commands) = QueryBuilder (commands ++ [str])
--  build (QueryBuilder commands) = commands

-- Example usage
main :: IO ()
main = do
--  let queryBuilder = QueryBuilder []
--  let updatedBuilder = queryBuilder
--    `appendString` "SELECT "
--    `appendString` (show $ Column "id" "users")
--    `appendString` ", "
--    `appendString` (show $ Column "name" "users")
--    `appendString` " FROM users"
--  let finalOutput = build updatedBuilder
--  putStrLn $ show finalOutput

  putStrLn "123"

